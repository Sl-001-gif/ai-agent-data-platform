<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aiagent.mapper.AnalysisSessionMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO analysis_session (user_id, dataset_id, title, analysis_goal, status)
        VALUES (#{userId}, #{datasetId}, #{title}, #{analysisGoal}, #{status})
    </insert>

    <select id="selectById" resultType="com.aiagent.entity.AnalysisSession">
        SELECT id, user_id, dataset_id, title, analysis_goal, status, create_time, update_time
        FROM analysis_session
        WHERE id = #{id}
    </select>

    <update id="update">
        UPDATE analysis_session
        <set>
            <if test="title != null">title = #{title},</if>
            <if test="analysisGoal != null">analysis_goal = #{analysisGoal},</if>
            <if test="datasetId != null">dataset_id = #{datasetId},</if>
            <if test="status != null">status = #{status},</if>
            update_time = NOW()
        </set>
        WHERE id = #{id}
    </update>

    <select id="countByUserId" resultType="long">
        SELECT COUNT(*)
        FROM analysis_session
        WHERE user_id = #{userId}
        <if test="keyword != null and keyword != ''">
            AND title LIKE CONCAT('%', #{keyword}, '%')
        </if>
        <if test="datasetId != null">
            AND dataset_id = #{datasetId}
        </if>
    </select>

    <select id="selectByUserId" resultType="com.aiagent.entity.AnalysisSession">
        SELECT id, user_id, dataset_id, title, analysis_goal, status, create_time, update_time
        FROM analysis_session
        WHERE user_id = #{userId}
        <if test="keyword != null and keyword != ''">
            AND title LIKE CONCAT('%', #{keyword}, '%')
        </if>
        <if test="datasetId != null">
            AND dataset_id = #{datasetId}
        </if>
        ORDER BY update_time DESC, id DESC
        LIMIT #{offset}, #{limit}
    </select>

    <delete id="deleteById">
        DELETE FROM analysis_session WHERE id = #{id}
    </delete>

    <delete id="deleteBatch">
        DELETE FROM analysis_session WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

</mapper>